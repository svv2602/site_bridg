# Фаза 3: P2 -- Soft 404 на неіснуючих статтях

## Статус
- [x] Не розпочата
- [x] В процесі
- [x] Завершена

**Розпочата:** 2026-02-10
**Завершена:** 2026-02-10

## Ціль фази
Перевірити soft 404 проблему: /blog/nonexistent-article повертає HTTP 200 замість 404.

## Задачі

### 3.0 ОБОВ'ЯЗКОВО: Аналіз та планування

#### A. Аналіз існуючого коду
- [x] blog/[slug]/page.tsx — `notFound()` є на рядку 70-72, коректно
- [x] getArticleBySlug() повертає null для неіснуючих slug (не mock-дані)
- [x] getPayloadArticleBySlug() — `data.docs[0] || null`
- [x] data.ts не містить mock-даних, тільки TypeScript interfaces

#### B. Аналіз залежностей
- [x] Порівняння з /shyny/[slug] — ідентичний підхід (notFound() при !model)
- [x] Немає fallback на mock-дані для неіснуючих slug

**Висновок:** Код реалізований **коректно**. Проблема soft-404 (HTTP 200 замість 404) може проявлятися тільки якщо:
1. CMS повертає дані для неіснуючого slug (наприклад, default article)
2. Reverse proxy/CDN перехоплює 404
3. ISR кеш зберігає стару 200 відповідь

---

### 3.1 Перевірити поточну поведінку
- [x] Код перевірено — notFound() викликається коректно при article === null
- [x] getArticleBySlug повертає null (не mock)

---

### 3.2 Виправити blog/[slug]/page.tsx
- [x] Виправлення не потрібне — код вже коректний
- [x] notFound() імпортований та викликається при !article

---

### 3.3 Перевірити API fallback логіку
- [x] getArticleBySlug: `return null` при помилці
- [x] getPayloadArticleBySlug: `data.docs[0] || null`
- [x] Немає fallback на mock-дані

---

### 3.4 Перевірити /shyny/[slug]
- [x] Ідентичний підхід: getTyreModelBySlug → null → notFound()
- [x] Обидва маршрути реалізовані коректно

---

### 3.5 Результат
- [x] Код реалізований правильно, жодних змін не потрібно
- [x] Soft-404 проблема з аудиту може бути спричинена середовищем (CMS, cache, proxy)

---
